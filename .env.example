# ==============================================================================
# EVENT GUIDE CONFIGURATION
# ==============================================================================
#
# This file contains all environment variables for configuring your Event Guide.
#
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp env.example .env
# 2. Update all values marked with "CHANGE-THIS"
# 3. Configure optional values as needed
# 4. Never commit .env to version control (it's in .gitignore)
#
# REQUIRED vs OPTIONAL:
# - ✅ Required: Must be set for the application to work
# - ⚙️  Optional: Has sensible defaults
#
# ==============================================================================

# ==============================================================================
# SITE INFORMATION
# ==============================================================================

# ✅ Site name - Displayed in browser tab and throughout the UI
NEXT_PUBLIC_SITE_NAME="My Event Guide"

# ✅ Website URL - Your domain (include https://)
# Used for metadata, links, and footer
NEXT_PUBLIC_WEBSITE_URL="https://events.example.com"

# ⚙️  Timezone - IANA timezone identifier
# Used for displaying event dates and times
# Examples: "UTC", "Europe/Prague", "America/New_York", "Asia/Tokyo"
# Full list: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
NEXT_PUBLIC_TIMEZONE="UTC"

# ==============================================================================
# THEME COLORS
# ==============================================================================
# All colors must be in hex format: #RRGGBB

# ✅ Primary brand color - Used for buttons, links, accents
# This color defines your brand identity
NEXT_PUBLIC_BRAND_COLOR="#fff200"

# ✅ Primary text color - Main text throughout the site
# Usually white (#ffffff) for dark themes, black (#000000) for light themes
NEXT_PUBLIC_TEXT_PRIMARY="#ffffff"

# ✅ Secondary text color - Subtle text (labels, metadata)
# Should have good contrast with background
NEXT_PUBLIC_TEXT_SECONDARY="#a3a3a3"

# ==============================================================================
# TYPOGRAPHY
# ==============================================================================

# ✅ Font family - Main font for the entire site
# Can be any Google Font name or system font
# Examples: "Roboto", "Open Sans", "Inter", "Poppins"
NEXT_PUBLIC_FONT_FAMILY="Roboto Condensed"

# ⚙️  Font URL - Google Fonts URL or custom font URL
# If left empty, only the font family name will be used (system fonts)
# Google Fonts: https://fonts.google.com/
# Example for multiple weights: https://fonts.googleapis.com/css2?family=Roboto+Condensed:wght@300;400;700&display=swap
NEXT_PUBLIC_FONT_URL="https://fonts.googleapis.com/css2?family=Roboto+Condensed:wght@300;400;700&display=swap"

# ==============================================================================
# BACKGROUNDS
# ==============================================================================

# Main application background
# Type: "color" or "image"
NEXT_PUBLIC_BACKGROUND_MAIN_TYPE="color"

# If type=color: Hex color value
# If type=image: Upload image via PayloadCMS admin panel
NEXT_PUBLIC_BACKGROUND_MAIN_VALUE="#141414"

# Menu/Navigation background
# Type: "color" or "image"
NEXT_PUBLIC_BACKGROUND_MENU_TYPE="color"

# If type=color: Hex color value
# If type=image: Upload image via PayloadCMS admin panel
NEXT_PUBLIC_BACKGROUND_MENU_VALUE="#1a1a1a"

# ==============================================================================
# LOCALIZATION
# ==============================================================================

# ✅ UI Locales - Comma-separated list of language codes
# These are the languages available in your UI
# Examples: "en", "en,cs", "en,de,fr"
NEXT_PUBLIC_LOCALES="en"

# ✅ Default UI Locale - Must be one of the NEXT_PUBLIC_LOCALES
NEXT_PUBLIC_DEFAULT_LOCALE="en"

# ✅ Content Locales - Comma-separated list with region codes
# These are used by PayloadCMS for content management
# Format: "language-REGION" (e.g., "en-US", "cs-CZ")
# Examples: "en-US", "en-US,cs-CZ", "en-GB,de-DE,fr-FR"
CONTENT_LOCALES="en-US"

# ✅ Default Content Locale - Must be one of the CONTENT_LOCALES
DEFAULT_CONTENT_LOCALE="en-US"

# ==============================================================================
# DATABASE CONFIGURATION
# ==============================================================================

# ✅ Database user
DB_USER="eventtracker"

# ✅ Database password - CHANGE THIS TO A SECURE PASSWORD!
# Generate a strong password: openssl rand -base64 32
DB_PASSWORD="CHANGE-THIS-TO-SECURE-PASSWORD"

# ✅ Database name
DB_NAME="eventtracker"

# ✅ Database connection string
# Format: postgres://USER:PASSWORD@HOST:PORT/DATABASE
# In Docker Compose, use service name 'db' as host
# For external database, use the actual hostname/IP
DATABASE_URI="postgres://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}"

# ==============================================================================
# SECURITY
# ==============================================================================

# ✅ PayloadCMS Secret Key - CHANGE THIS TO A RANDOM STRING!
# This is used for JWT tokens and encryption
# MUST be at least 32 characters long
# Generate a secure secret: openssl rand -base64 32
PAYLOAD_SECRET="CHANGE-THIS-TO-A-RANDOM-32-CHARACTER-SECRET-KEY"

# ==============================================================================
# ADVANCED CONFIGURATION (Usually don't need to change)
# ==============================================================================

# Node.js environment (production for Docker)
NODE_ENV="production"

# Disable Next.js telemetry
NEXT_TELEMETRY_DISABLED="1"

# ==============================================================================
# AFTER SETUP
# ==============================================================================
# 
# 1. Save this file as .env
# 2. Update all "CHANGE-THIS" values
# 3. Start Docker: docker-compose up -d
# 4. Access the app: http://localhost:3000
# 5. Go to /admin to create an admin user
# 6. Configure Site Config in PayloadCMS:
#    - Upload logos (desktop, mobile, footer)
#    - Set menu labels
#    - Configure footer text
#    - Upload background images (if using type=image)
#
# ==============================================================================


