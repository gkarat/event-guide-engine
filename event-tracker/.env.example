# ==================================
# EVENT GUIDE CONFIGURATION
# ==================================
# This file contains DEPLOYMENT-LEVEL configuration
# For media assets and UI text, use PayloadCMS admin panel

# ----------------------------------
# SITE INFORMATION (Required)
# ----------------------------------
NEXT_PUBLIC_SITE_NAME="My Event Guide"
NEXT_PUBLIC_WEBSITE_URL="https://events.example.com"
NEXT_PUBLIC_TIMEZONE="UTC"  # IANA timezone (e.g., Europe/Prague, America/New_York)

# ----------------------------------
# THEME COLORS (Required)
# ----------------------------------
# Define your brand colors using hex format
NEXT_PUBLIC_BRAND_COLOR="#fff200"
NEXT_PUBLIC_TEXT_PRIMARY="#ffffff"
NEXT_PUBLIC_TEXT_SECONDARY="#a3a3a3"

# ----------------------------------
# TYPOGRAPHY (Required)
# ----------------------------------
NEXT_PUBLIC_FONT_FAMILY="Roboto Condensed"

# Optional: Custom font URL (Google Fonts or self-hosted)
# Leave empty to use system fonts only
NEXT_PUBLIC_FONT_URL="https://fonts.googleapis.com/css2?family=Roboto+Condensed:wght@300;400;700&display=swap"

# ----------------------------------
# BACKGROUNDS (Required)
# ----------------------------------
# Main application background
NEXT_PUBLIC_BACKGROUND_MAIN_TYPE="color"      # Options: "color" or "image"
NEXT_PUBLIC_BACKGROUND_MAIN_VALUE="#141414"   # Hex color (if type=color)

# Menu/navigation background
NEXT_PUBLIC_BACKGROUND_MENU_TYPE="color"      # Options: "color" or "image"
NEXT_PUBLIC_BACKGROUND_MENU_VALUE="#1a1a1a"   # Hex color (if type=color)

# Note: If type="image", upload the image via PayloadCMS admin panel
# The image will be loaded from the site-config global

# ----------------------------------
# LOCALIZATION (Required)
# ----------------------------------
# UI locales (comma-separated, e.g., "en,cs,de")
NEXT_PUBLIC_LOCALES="en"
NEXT_PUBLIC_DEFAULT_LOCALE="en"

# Content locales for PayloadCMS (with region codes)
CONTENT_LOCALES="en-US"
DEFAULT_CONTENT_LOCALE="en-US"

# ----------------------------------
# DATABASE (Required)
# ----------------------------------
DB_USER="eventguide"
DB_PASSWORD="change-this-secure-password"
DB_NAME="eventguide"
DATABASE_URI="postgres://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}"

# ----------------------------------
# SECURITY (Required)
# ----------------------------------
# Generate with: openssl rand -base64 32
PAYLOAD_SECRET="change-this-to-a-random-32-character-string"

# ==================================
# AFTER DEPLOYMENT:
# ==================================
# 1. Login to /admin
# 2. Navigate to Globals > Site Config
# 3. Upload your logos (desktop, mobile, footer)
# 4. Configure UI text labels
# 5. If using image backgrounds, upload them there